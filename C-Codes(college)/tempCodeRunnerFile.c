#include "stdio.h"
int main(){
    char str[30]="hello";
    // printf("%.2s",str);
    // printf("%10.4s sex\n",str);
    // printf("&str" );
    int c=0,i=0;
    while(str[i]!='\0')
    {
        c++;
        i++;
    }
}